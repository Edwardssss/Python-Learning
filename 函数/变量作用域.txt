# 局部变量：函数内赋值且无特殊声明的变量，只能在当前函数使用
# 全局变量：在函数外赋值的变量，可以在整个程序范围内被访问

# 注意：如果在函数内部，局部变量和全局变量的标识符一致，则函数内取局部变量的值，函数外部仍为全局变量
# 若要在函数内部修改全局变量，则需要global关键字（如 global x)

# lambda是匿名函数，其结构为lambda 参数:返回值
# filter是过滤序列的函数，filter(某函数作为筛选条件,筛选范围)
