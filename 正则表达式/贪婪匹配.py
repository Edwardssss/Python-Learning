# 贪婪模式指的是尽可能多的匹配字符串
# 非贪婪模式指的是尽可能少的匹配字符串
# 默认情况下限定符都是贪婪匹配，在限定符后面加上?表示非贪婪匹配
import re
m1 = re.match(r'[1-9]+', string='134668')  # 贪婪匹配
m2 = re.match(r'[1-9]+?', string='134668')  # 非贪婪匹配
print(m1.group())
print(m2.group())
